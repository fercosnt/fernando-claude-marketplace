{
  "name": "clinicorp-mcp-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Fonte do servidor MCP do plugin clinicorp. O artefato distribuido e ../servers/clinicorp-mcp.js (bundle sem dependencias).",
  "scripts": {
    "build": "tsc --noEmit && esbuild src/index.ts --bundle --platform=node --target=node18 --format=esm --outfile=../servers/clinicorp-mcp.js --banner:js=\"#!/usr/bin/env node\"",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "allowScripts": {
    "esbuild@0.24.2": true
  }
}
